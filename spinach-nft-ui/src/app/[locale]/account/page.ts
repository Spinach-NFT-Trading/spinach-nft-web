import {AccountIndex} from '@spinach/next/ui/account/index/main';
import {generatePageMeta} from '@spinach/next/utils/website/meta/main';


export const generateMetadata = generatePageMeta({key: 'Account.Index.Title'});

export default AccountIndex;
