import {AccountIdVerify} from '@spinach/next/ui/account/verify/id/main';
import {generatePageMeta} from '@spinach/next/utils/website/meta/main';


export const generateMetadata = generatePageMeta({key: 'Account.Verify.Id.Title'});

export default AccountIdVerify;
