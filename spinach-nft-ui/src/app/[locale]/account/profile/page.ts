import {AccountProfile} from '@spinach/next/ui/account/profile/main';
import {generatePageMeta} from '@spinach/next/utils/website/meta/main';


export const generateMetadata = generatePageMeta({key: 'Account.Profile.Title'});

export default AccountProfile;
