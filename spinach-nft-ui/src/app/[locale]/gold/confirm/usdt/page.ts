import {GoldExchangeConfirmUsdt} from '@spinach/next/ui/gold/confirm/usdt/main';
import {generatePageMeta} from '@spinach/next/utils/website/meta/main';


export const dynamic = 'force-dynamic';

export const generateMetadata = generatePageMeta({key: 'Gold.Confirm.Usdt.Title'});

export default GoldExchangeConfirmUsdt;
