import {GoldExchangeConfirmTwBank} from '@spinach/next/ui/gold/confirm/twBank/main';
import {generatePageMeta} from '@spinach/next/utils/website/meta/main';


export const generateMetadata = generatePageMeta({key: 'Gold.Confirm.TwBank.Title'});

export default GoldExchangeConfirmTwBank;
